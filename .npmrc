# 依赖统一从公网镜像获取，避免 lock 文件里混入内网 registry 地址
# 如需官方源，改成 https://registry.npmjs.org
registry=https://registry.npmmirror.com

# npmmirror 未实现 /-/npm/v1/security/* 接口，安装时会报 404，关掉噪音
# 需要审计时单独执行：npm audit --registry=https://registry.npmjs.org
audit=false
